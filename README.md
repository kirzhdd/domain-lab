# domain-lab
my study windows domain + trust

## theory
что нужно для подготовки сервера перед развертыванием домена и как это сделать на
windows server

статическая ip-адресация - мы вручную прописываем постоянный ip устройству, вместо того
чтобы его назначали динамически. это нужно для того, чтобы все знали точный адрес днс-сервера,
а роль днс сервера играет контроллер домена. так для всех остальных компьютеров в домене
днс-сервер это контроллер, а для самого контроллера - это он сам

днс-пересылка - это когда сервер не может сам ответить на запрос и пересылает его другому серверу \
есть два типа пересылок:
1. обычная пересылка (forwarders) - всё, что не обрабатывается переслать на другой днс-сервер
2. условная пересылка (conditional forwarders) - только запросы для конкретных доменов летят на 
конкретный днс-сервер

виды доверия:\
external - связывает два конкретных домена из разных лесов\
forest - связывает два леса

проверка подлинности в лесу - означает, что как только пользователь из другого леса прошёл
аутентификацию в моем лесу, то он попадает в "Authenticated Users", которым доступно всё в моем лесу\
выборочная проверка - идёт разграничение, что в каком лесу кому доступно

## последовательность моих действий
1. настроить статический ip на кд
2. переименовать компьютер
3. через диспетчер серверов развернуть домен
4. настроить условную днс-пересылку между доменами
5. проверить что всё ок
6. теперь можно добавлять доверие - через виндовский кд или samba-tool
7. проверить что добавилось доверие

(!) лучше external trust чем forest trust потому что на альт-домене sssd, а 
forest trust не поддерживается sssd

8. поэтому удаляем старое доверие и делаем новое
9. добавить в test.win рабочую станцию


## проверить что доверие работает
1. Kerberos должен видеть второй домен
2. войти с учетной записи альт домена в виндовс домен\
проблема - ```Чтобы войти удалённо в систему вам нужно право на вход через службу удалённых рабочих столов...``` -> добавить право на вход и войти

